apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules:
  - host: 192.168.1.118
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cats-dogs-review-app
            port:
              name: http
